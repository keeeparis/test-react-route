import React from 'react'
import { mount } from '@cypress/react'
import Item from '../components/Item'

describe('Testing Item component', () => {
    it('Item renders with custom props' , () => { // how it works testing the component
        // mount(<Item />)
    })
})